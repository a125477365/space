services:
  space:
    image: space-orbital-tracker:latest
    build: .
    container_name: space-tracker
    ports:
      - "8080:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost/"]
      interval: 30s
      timeout: 3s
      start_period: 5s
